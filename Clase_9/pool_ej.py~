from multiprocessing import Process, Pool
import time
import os

def foo(t):
    time.sleep(t)
    print('H: ' + str(os.getpid()) + ' ' + str(t))
    

pool = Pool(processes=4)

pool.map(foo, [0,1,2,3])#,4,5,6])


